extends layout

block content
  include partials/navigation
  main(class="container pt-4 col-12")
    h1.text-center "Bienvenido a nuestra vibrante comunidad donde las experiencias se comparten y las motivaciones se celebran. Aquí puedes descubrir historias inspiradoras, metas ambiciosas y valiosas lecciones de vida compartidas por personas de todo el mundo. Explora, conecta y déjate inspirar. ¿Quién sabe? Quizás encuentres la chispa que necesitas para dar el siguiente paso en tu propio viaje."
    div(class="col-lg-12")
      ul(class="task-list row")
        if tasks.length === 0
          li
            b.text-start No tienes tareas por hacer 😃👌🏻
        else
          each task in tasks
            div(class="header-post bg-color")
              div(class="container-flud")
                span #{name}
            li #{task.title}
            div(class="footer-post bg-color")
              div(class="container-flud")
                span(class="text-start") #{fullDate}
                  div(class="placement")
                      div(class="heart")
